<view class="bind-page">
  <view class="bind-card">
    <view class="bind-icon">💬</view>
    <text class="bind-title">绑定微信</text>
    <text class="bind-desc">绑定微信后，下次可直接使用微信一键登录</text>
    <button class="bind-btn" loading="{{binding}}" bindtap="bindWechat">绑定微信</button>
    <text class="skip-text" bindtap="skipBind">跳过，暂不绑定</text>
  </view>
</view>
